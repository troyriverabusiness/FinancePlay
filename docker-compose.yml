services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    container_name: financeplay-server
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    # Keep container running
    stdin_open: true
    tty: true

